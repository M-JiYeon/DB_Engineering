WHERE 절까지 읽은 다음 GROUP BY 절 읽어

데이터베이스 장점 : 데이터 통합관리 용이 / 보안 / 등등
데이터베이스 단점 : 전문적인 인력만 사용 가능. 인력 부족. 유지비용이 많이 들어. 라이센스...

HAVING : GROUP BY 다음으로 실행하는 조건.

서브 질의 / 그룹함수

용어 - 2가지 방법
1) 사전적 의미
2) 나의 정의

유일키. 다른 속성과 구분이 되는 속성. 유니크. 하나밖에 없어

DB에 접근하는 것을 세션을 얻었다고 한다.
메모리 안에서만 명령 수행이 이루어져.
작업 내용을 적용 / 저장 = commit
rollback하면 마지막 commit 위치로 돌아가.
한 DB를 여러 유저가 접속할 수 있어.
메모리 상에 각 유저 별 공간이 있어.
내가 작업한 내용을 다른 사람이 보지는 못해. -> COMMIT으로 적용시키면 가능.
COMMIT이 있는 이유..? : 혹시나 오류가 있을 경우 되돌리기 하라고.

INNER JOIN - 조인의 결과가 NULL일 때 결과를 보여주지 않음.
	|-- SELF JOIN : 자기 자신 조인
	|-- EQUI JOIN : 동등비교로 조인
	|-- NON EQUI JOIN : 범위 조인? ex) 등급 급여 범위..?

OUTER JOIN - NULL로 보여줄 쪽 (+)
	

SQL에서는 조건문과 반복문 문법은 없어
오라클에서 제공하는언어 ; 프로시저 SQL?
개발자가 직접 개발하는 언어

OR 사용 시 주의 : OR의 범위를 위해 ()로 표시해 줘

ANSI 조인 : FROM ... JOIN  .. ON
where 절은 일반 조건 적고 / on에 조인 조건 명시
nature join : 똑같은 이름의 컬럼이 있으면 자동조인 / 단, 같은 컬럼이름이 2개 이상이면 따로 명시해줘야함,


SELECT절 서브 질의 : 무조건 단일 컬럼 / 단일 행(값)

WHERE절 서브 질의 : 다중값 비교 연산자에서 사용가능(IN / ANY, ALL, SOME / EXISTS 등) - 단일 컬럽 / 여러 행


데이터의 중복을 피하려고 분리를 해놨는데 쓸 때는 분리되어 있는 데이터를 한번에 꺼내고 싶어.
그래서 조인을 거의 무조건 써야해.
조인을 하려면 서브질의 / 그룹함수를 잘 알아야 해.
많이 사용해 봐.

조인 정의
서브쿼리 정의
그룹 함수 정의 / GROUP BY
트랜잭션 정의


조인이란
두 개 이상의 테이블을 합쳐 새로운 가상의 테이블을 만드는 것
RDBMS에서 데이터의 중복을 피하기 위해 분리한 여러 테이블에서 한번에 자료를 가져오기 위한 연산.

조인의 종류는 
INNER JOIN
OUTER JOIN

INNER JOIN의 경우 자기 자신 / 동등 / 범위? 조인있음
OUTER JOIN은 오라클에서 (+)를 붙여서 표현.

ANSI JOIN은 표준 JOIN문법.

서브쿼리

