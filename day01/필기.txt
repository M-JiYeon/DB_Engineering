10/18
전은석 : 010-3175-9042
서버 : 서비스를 제공하는 것

질의 명령 = SQL = 쿼리문

MySQL : 유저가 따로 있고 DB에 테이블 존재
Oracle : DB에 유저가 종속되어 있고 유저에 각각 테이블 존재

java -version : 자바 버전 확인
javac -version :  자바 컴파일러 버전

환경 변수 : 환경에 필요한데 그 값이 변할 수 있는 데이터

시스템 계정 : 데이터 베이스 전반을 관리하는 계정

정상적으로 종료가 안되면 커밋이 안됨. 여태까지 작업 자동 적용 x.

DML(이미 만들어진 테이블을 다루는 것) - insert, select, update, delete
DCL - 
DDL(DB에 뭔가를 만드는 것) - CREATE, ALTER, DROP

접속 - 세션을 얻었다라고 표현

ALTER USER hr IDENTIFIED BY hr ACCOUNT UNLOCK;

오라클 : db를 관리해주는 시스템
sqlplus : sql문을 전달해주는 시스템
sqldeveloper : db 쉽게 보는 도구

데이터 : 현실세계에서 관측한 값들. 하나하나가 데이터인데 그것만 가지고 의미를 가질 수 없어. db에서는 한 행을 데이터 1개라고 의미.
	
정형 데이터베이스 : 형태가 정해진 데이터
비정형 데이터베이스 : 형태가 정해져있지 않고 거의 문자열로 처리

작업을 분산해서 처리하면 작업 속도가 빨라짐.??? 졸려서 제대로 못들음. 검색
웹서비스 : 웹서버 + 데이터베이스 함까 사용

기본키를 알면 다른 데이터를 모두 알 수 있어야 해.

****무결성 데이터

도메인 : 한 속성이 가질 수 있는 모든 값들.

ER다이어그램 : 엔티티(테이블) 릴레이션. 테이블 간의 관계를 보여주는 다이어그램.
	       컬럼들과 엔티티와 관계뿐만 아니라 엔티티와 엔티티 간의 관계도 형성.

스키마 : 데이터베이스의 전체적인 구조.

파일 : 중복 데이터 피할 수 없음. 수정 어려움. 보안 문제
데베 사용. 

SQL : structure query langueage. 구조화 되어있는 쿼리 언어. 쿼리-콩깍지. 

오라클의 단점은 가격 / 구 버전 찾기 어려움.

쿼리를 짤 때 where절에서 제일 많이 걸러내는 순으로 오는 것이 좋아.

서브쿼리는 어디서든 사용 가능하나 쓰는 위치에 따라 결과의 컬럼 수 등 고려할 필요가 있다.
select 절에서는 결과가 1개
from 절은 테이블 가능. 별칭 사용해야 함

오라클에서는 문자와 문자열 따로 구분 x
오라클 데이터 타입
-문자-
	char 	- 최대 4kbyte 고정길이형 데이터 타입 
	varchar2	- 최대 4kbyte 가변길이형 데이터 타입
-숫자-
	number	- 숫자 데이터
-날짜-
	date	- 날짜 데이터

	clob	- 4GB까지 저장 가능한 문자열 데이터
	blob	- 4GB까지 저장 가능한 바이너리 데이터

데이터 형변환
	숫자데이터 <--------> 문자데이터 <---------> 날짜데이터

별칭의 의미와 데이터 결합, 타입 변환.